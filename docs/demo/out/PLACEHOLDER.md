# No product demo in this folder

`plugin-demo.mp4` and `plugin-demo.gif` are not here on purpose.

A Pillow/ffmpeg animation is not the demo. A Linux Playwright pass that never mounts the plugin dock is not the demo either.

Record on macOS Apple Silicon using [../RECORDING.md](../RECORDING.md), then put the real files in this folder.
