# Placeholders — not the product demo

`plugin-demo.mp4` and `plugin-demo.gif` in this folder are a labeled Pillow/ffmpeg animation. They are **not** a recording of DeepSeek Harness Web UI.

Do not post them on X, embed them as the README demo, or merge them as the product video.

Replace them by dropping real screen recordings into `docs/demo/raw/` (`01-drag-image.mov` … `08-on-demand.mov` or `.mp4`) and running `docs/demo/assemble.sh`.
